import { Cookie } from 'lucide-react';
import { LegalPage } from './LegalPage';

const CookiePolicy = () => (
  <LegalPage translationKey="cookies" icon={Cookie} canonicalPath="/cookies" />
);

export default CookiePolicy;
