import { Scale } from 'lucide-react';
import { LegalPage } from './LegalPage';

const TermsOfService = () => (
  <LegalPage translationKey="terms" icon={Scale} canonicalPath="/terms" />
);

export default TermsOfService;
