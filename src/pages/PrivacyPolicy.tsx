import { ShieldCheck } from 'lucide-react';
import { LegalPage } from './LegalPage';

const PrivacyPolicy = () => (
  <LegalPage translationKey="privacy" icon={ShieldCheck} canonicalPath="/privacy" />
);

export default PrivacyPolicy;
